# T-Pay front webapp

## Build Setup

``` bash
# install dependencies
$ npm install

# dev
$ npm run dev

# build
$ npm run build

```

or 

```bash
# build docker containers
$ docker-compose build

# run
$ docker-compose up -d
```